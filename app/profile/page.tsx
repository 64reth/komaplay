/* eslint-disable react-hooks/error-boundaries, @typescript-eslint/no-explicit-any, react-hooks/purity */
import Link from 'next/link';import {communityIdentity} from '../../lib/open-panel/server';import {handbookState} from '../../lib/handbook/server';export const dynamic='force-dynamic';export default async function Profile(){try{const {db,user,profile}=await communityIdentity(false,"/profile");const [handbook,contributions,citations]=await Promise.all([handbookState(db,user.id),db.from('contributions').select('id,status,feature_id,created_at').eq('author_id',user.id),db.from('panel_citations').select('id,revision_number').eq('public_credit',profile.display_name)]);return <main className="editorial-page"><div className="op-workspace profile-page"><p className="op-eyebrow">PANEL IDENTITY</p><h1>{profile.display_name}</h1><p>Member since {new Date((profile as any).created_at??Date.now()).toLocaleDateString('en-GB')}</p><p>{handbook.status==='accepted'?'Current handbook accepted.':'Handbook acceptance required before Workshop participation.'}</p><p>{contributions.data?.filter(c=>c.status==='Accepted').length??0} published contributions · {citations.data?.length??0} Panel Citations</p><p><Link href="/profile/settings">SETTINGS →</Link></p><section id="contributions"><h2>My contributions</h2>{(contributions.data??[]).map(c=><p key={c.id}>{c.status} · <Link href={`/features/tokon/workshop`}>View Workshop</Link></p>)}</section></div></main>}catch(e){return <main className="editorial-page"><div className="op-workspace"><h1>Sign in to view your profile</h1><Link href="/features/tokon/workshop">SIGN IN</Link></div></main>}}
